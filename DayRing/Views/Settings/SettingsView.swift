import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationStack {
            Text("设置")
                .navigationTitle("设置")
        }
    }
}

#Preview {
    SettingsView()
}
